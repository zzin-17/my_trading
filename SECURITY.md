# 계정·보안 정리 (TraderOS Portfolio Visual)

## 계정 / 로그인

- **서버 측 계정·로그인 없음.** 앱은 정적 프론트엔드 + 브라우저 저장만 사용합니다.
- 배포 시에도 사용자 인증을 가정하지 않습니다.

## 로컬에 저장되는 데이터

| 저장 위치 | 내용 |
|-----------|------|
| `localStorage` 키 `traderos-portfolio-v2` | 매매일지(거래), 시세, 포지션 ID, 할 일, 메모, 한국장 수수료 설정 등 (`src/lib/persistence.ts`) |

- **민감 정보(비밀번호, API 키, 증권사 계좌번호)를 앱이 수집·저장하지 않습니다.**
- 공용 PC·브라우저에서는 다른 사람이 동일 프로필로 접근할 수 있으므로, 필요 시 브라우저 데이터 삭제 또는 시크릿 모드를 사용하세요.

## 네트워크 / 외부 연동

- **한국 시세·KRX 목록**은 개발 서버에서 Vite 미들웨어(`/api/kr-quote`, `/api/krx-kind`)를 통해 공개 페이지를 조회합니다. 저장소에 **API 키·토큰을 두지 않습니다.**
- 프로덕션에서 프록시 URL을 바꿀 때만 선택적으로 사용:
  - `VITE_KR_QUOTE_BASE` — 시세 API 베이스 URL (기본: `/api/kr-quote`)
  - `VITE_KRX_PROXY_BASE` — KRX 목록 프록시 베이스 (기본: `/api/krx-kind`)
- 위 값은 **비밀이 아닌 공개 엔드포인트 경로**용이며, 실제 비밀번호/키는 필요하지 않습니다.

## 저장소·협업 시 권장 사항

1. **`.env`, `.env.local`은 커밋하지 않음** (`.gitignore`에 포함).
2. 팀과 공유할 변수 이름만 필요하면 **`.env.example`**에 키만 적고 값은 비워 둡니다.
3. 이슈·PR에 **실제 매매 금액·전체 매매일지 JSON**을 붙이지 않기.

## 문의

보안 취약점을 발견하면 비공개 채널로 알려 주세요.
